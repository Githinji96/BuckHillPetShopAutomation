{
  "name": "buckhill_petshopui_test",
  "version": "1.0.0",
  "description": "E_commerce functionality tests",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:run:chrome": "cypress run --browser chrome --headless",
    "cypress:run:electron": "cypress run --browser electron --headless",
    "cypress:run:firefox": "cypress run --browser firefox --headless",
    "cypress open": "cypress open"
  },
  "author": "Bonface Githinji",
  "license": "ISC",
  "dependencies": {
    "D": "^1.0.0",
    "cypress": "^12.7.0"
  },
  "devDependencies": {
    "cypress-xpath": "^2.0.1"
  }
}
